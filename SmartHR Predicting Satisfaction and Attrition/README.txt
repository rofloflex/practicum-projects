Сборный проект — 2: Предсказание удовлетворённости и увольнений сотрудников

Описание проекта
Компания «Работа с заботой» занимается HR-аналитикой и помогает бизнесу оптимизировать управление персоналом. 
В этом проекте ставится цель использовать машинное обучение для решения двух ключевых задач:

Предсказать уровень удовлетворённости сотрудника работой.
Предсказать факт увольнения сотрудника.

Цель проекта
Увеличить точность и скорость аналитических выводов.
Снизить риск потери сотрудников за счёт раннего выявления неудовлетворённости и вероятности увольнения.
Сформировать персональные рекомендации для HR-службы.

Задача 1: Предсказание уровня удовлетворённости работой
Данные
Целевая переменная: job_satisfaction_rate (от 0 до 1)

Признаки:
id, dept, level, workload, employment_years, last_year_promo, last_year_violations, supervisor_evaluation, salary

Этапы:
Загрузка и предобработка данных.

Исследовательский анализ признаков.

Построение пайплайна с кодированием категориальных признаков (OneHotEncoder, OrdinalEncoder) и масштабированием (StandardScaler, MinMaxScaler).

Обучение моделей:

Линейная модель
Дерево решений
Подбор гиперпараметров (GridSearchCV, RandomizedSearchCV)

Оценка качества модели с помощью метрики SMAPE:

𝑆𝑀𝐴𝑃𝐸=100𝑛∑𝑖=1𝑛∣𝑦𝑖−𝑦^𝑖∣(∣𝑦𝑖∣+∣𝑦^𝑖∣)/2
SMAPE=n100i=1∑n(∣yi ∣+∣ y^i ∣)/2∣y i − y^i ∣
 
Сравнение результатов, выбор лучшей модели.
Целевой критерий: SMAPE ≤ 15 на тестовой выборке.

Задача 2: Предсказание увольнения сотрудника

Данные
Целевая переменная: quit (0 — остался, 1 — уволился)
Используются те же входные признаки, что и в первой задаче.
Дополнительно добавляется job_satisfaction_rate, предсказанный моделью из первой задачи.

Этапы:
Загрузка и предобработка данных.

Исследование:

Анализ «портрета» уволившегося сотрудника.
Сравнение распределений job_satisfaction_rate для оставшихся и уволившихся.
Обогащение признаков предсказанным уровнем удовлетворённости.

Обучение моделей:

Подбор гиперпараметров

Оценка качества модели:

Используется метрика ROC-AUC

Цель: ROC-AUC ≥ 0.91 на тестовой выборке

Результаты и выводы:
Вывод по 1 задаче: Обучили несколько моделей, лучшая 'models', RandomForestRegressor(random_state=42), с метрикой SMAPE равной 14 на тренеровочной выборке. Тестовая выборка показала результат 12. Что меньше 15 и подходит нам по заданию.

Вывод по 2 задаче: Обучили несколько моделей, лучшая 'models' DecisionTreeClassifier(max_depth=6, max_features=6, random_state=42), с метрикой ROC_AUC, которая показала на тестовой выборке значение 0.91, что удовлетворяет нашей задаче.

Выполнили 2 задачи. 1 задача - построить модель, которая будет предсказывать уровень удовлетворенности работников. 
2 задача - построить модель, которая будет предсказывать, уволится сотрудник или нет, 
что позволит уменьшит текучку кадров в фирме. При решении задач были проведены следующие этапы: 
Загрузка данныХ. обработка, анализ и подготовка данных к обучению модели и само обучение. 
Вот некоторые рекомендации: Больше всего уволилось сотрудников из отдела продажь, а если относительно кол-ва то закупщики. 
Стоит узнать в чем дело, что не устраивает сотрудников. В среднем зарплаты меньше у уволившихся на 15к, 
возможно стоит увеличить зарплаты.

Рекомендации для бизнеса
Использовать предсказанную удовлетворённость для раннего выявления рисков.
Внедрить систему приоритетного внимания к сотрудникам с высокой вероятностью увольнения.
Автоматизировать регулярный анализ с использованием предложенной модели.

Используемые технологии
Python, Pandas, NumPy

Scikit-learn (Pipeline, ColumnTransformer, модели, GridSearchCV)

Визуализация: Matplotlib, Seaborn

Метрики: SMAPE, ROC-AUC